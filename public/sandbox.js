"use strict";
// Type Aliases : prevent code duplication / redundancy. 
// we can declare a particular type & store it & re-use it as per our own needs.
var logDetails = function (objNinja) {
    console.log("Name is : " + objNinja);
};
logDetails('Luigi');
